import { createStore } from "vuex";

// import LoginModuleStore from "@/login-module/services/login.store";

// export default createStore(
//     {
//         strict: process.env.APP_ENV !== "production" ? true : false,
//         modules: {
//             loginModule: LoginModuleStore,
//         }
//     }
// );

export default createStore({})